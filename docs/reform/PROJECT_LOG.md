# リフォーム営業SaaS プロジェクトログ(意思決定・実装状況の全記録)

このドキュメントは `PLANNING.md`(初期の企画・要件定義)以降の議論・決定事項・実装状況・未対応の要望をすべて集約したものです。次にこのプロジェクトに着手する際は、まずこのファイルを読めば経緯が追えるようにしています。

対象リポジトリ: `marushuuu/inbound`
作業ブランチ: `claude/reform-product-planning-lv0tli`
アプリ本体: `reform-app/`(Next.js, リポジトリ内の独立プロジェクト)。現在の版数: **v0.6**(画面右上/サイドバー下部に表示)
業界ナレッジからの更新方針: [`INSIGHTS_AND_ROADMAP.md`](./INSIGHTS_AND_ROADMAP.md)(2026-09-01 追加。v0.6以降のバックログ)
デザインモック: `docs/reform/assets/*.dc.html` + [Claude Design キャンバス](https://claude.ai/code/artifact/496d6f1c-0846-4f1b-b0db-e4d549048361)

---

## 1. プロダクト概要(確定)

リフォーム会社の**営業担当**が使う SaaS。スコープは **現場調査(ヒアリング)〜 契約** まで。

- 販売形態: リフォーム会社向けSaaS(自社ツールではない)
- 主要ユーザー: 営業担当(現場はスマホ/タブレット、社内はPC)
- 対象工事: **Fast系(水回り・住設交換)から着手**。リノベ系(間取り変更・3Dスキャン要)は後続フェーズ
- 参考データ: 実際の見積書PDF(浴室・洗面改装工事 ¥1,903,000)、業界KPI・業務フロー資料、ANDPAD技術ブログ

### 1-1. Fast系の業務フロー(確定・実装済み)

```
ヒアリング → (簡易現調) → 見積作成(松竹梅) → 施工日時の調整 → 契約締結
```

※当初「見積 → 契約」だった導線に、**「施工日時」ステップを追加**(契約前に担当者の空き状況を見ながら施工日を確定させたいという要望による)。現在のステップ順は **ヒアリング → 見積 → 施工日時 → 契約**。

### 1-2. リノベ系(後続フェーズ・未着手)

```
ヒアリング → 3Dスキャン → (イメージ作成 → 図面起こし → 積算) → 見積(スコープ型松竹梅) → 契約
```

Fast系と基盤(見積・契約・検索)を共有し、3Dスキャン〜積算工程のみ追加する設計。

---

## 2. 解決する課題(優先順・確定)

| # | 課題 | プロダクトでの解決 |
| --- | --- | --- |
| 1 | 見積作成に2〜8時間かかる | 過去見積DB検索(類似度スコア順)・松竹梅テンプレート・工事マスタで数クリック概算 |
| 2 | 上位グレード商品・画像をその場で出せない | 商品マスタにグレード系列を持たせ、松竹梅を自動生成。グレード比較モーダルで即座に提示 |
| 3 | 契約までのリードタイム | 見積 → 施工日時仮押さえ → その場で契約書生成・手書きサイン |
| 4 | 過去見積が資産化されていない | 見積を構造化して蓄積。工事種別×築年数×予算帯の類似度スコアでレコメンド |
| 5 | 工事日がその場で示せない | 担当者×時間枠のタイムグリッドで空き状況を可視化し、その場で仮押さえ |

---

## 3. データモデル(確定・実装済み)

サンプル見積書の構造(表紙 → 内訳 → 工事種別ごとの大項目 → 明細行)を反映。

- **Project(案件)**: 顧客名、工事タイトル、物件情報(築年数・構造)、ステータス、ヒアリング、選択中の見積パターン、設備グレードの差し替え、実施工事項目(taskIds)、施工日時(schedule)、契約情報
- **Hearing(ヒアリング)**: きっかけ・悩み(複数選択)、予算帯、予算上限、希望時期、過去のリフォーム履歴、キーマン同席、変動リスクメモ ※**このうち複数項目に変更要望あり(§7参照、未実装)**
- **Product(商品マスタ)**: カテゴリ、メーカー、商品名、型番、価格、グレード順位・ラベル、特徴。カテゴリ内でグレード系列(松竹梅の元)を構成
- **WorkLine(見積明細行)**: 工事種別の大項目 × 名称・仕様・金額(サンプル見積書の実データを移植)
- **WorkItem(工事マスタ)**: 工事項目ごとの**所要時間(分単位)**、工事種別、職種(担当者マッピング用)。施工日時の自動割付の元データ
- **Worker(担当者)**: 氏名、職種、自社/協力業者の別
- **ScheduleState(施工日時)**: 日付、開始/終了(分)、担当者ごとの割付タスク、Googleカレンダーの仮予定イベントID
- **ContractState(契約)**: 説明確認チェック3項目、発注者の署名(Canvas手書き)、締結時の会社情報スナップショット、締結日時
- **原価・粗利(v0.6)**: `WorkItem.costPrice`(標準原価)、`Product.costPrice`(仕入額)、`EstimateLine.costUnitPrice`(原価単価)、`CompanyProfile.targetMarginRate`(粗利目標%)。受注時粗利 = 値引き後の税抜提示額 − 原価合計
- **LostRecord(失注・v0.6)**: 区分(他決/検討中止)× 理由(価格/時期/プラン/その他)、競合先、備考、失注日
- **PastEstimate(過去見積)**: 検索・類似度スコアリングの対象データ

---

## 4. 技術方針(確定)

- **ANDPAD連携は言語非依存**: ANDPADのREST API経由になるため、こちらの実装言語は連携可否に影響しない。連携しやすさは「API層を疎結合に切り出すこと」「ANDPAD側のデータモデルに写像しやすいスキーマにすること」で決まる
- **採用スタック**: Next.js (App Router) + TypeScript + Tailwind CSS v4(本リポジトリの既存プロダクトと統一)。ANDPAD自身もTypeScriptを採用しており親和性が高い
- **DB**: 現状は localStorage のみの**フロントエンドMVP**(認証・DBは未導入)。将来的に PostgreSQL(+ pgvector で類似検索)を導入予定
- **連携・非同期処理層**: 将来 Go でマイクロサービスとして切り出す構想(ANDPADの技術方針に合わせる)。現時点ではNext.jsのAPI Routesで実装(Googleカレンダー連携など)
- **署名**: 発注者(お客様)はCanvas手書きサイン(MVP実装済み)。請負者(自社)は署名ではなく**会社情報+印鑑画像(社印)**で記名押印を表現する(§8-3-5)。電子契約サービス(クラウドサイン等)への連携は将来検討

---

## 5. デザインモック(Claude Design キャンバス)

[https://claude.ai/code/artifact/496d6f1c-0846-4f1b-b0db-e4d549048361](https://claude.ai/code/artifact/496d6f1c-0846-4f1b-b0db-e4d549048361)

配色は **ANDPAD系のバーミリオンレッド(#e33e21)× ダークチャコール × アンバー** に統一。ソースは `docs/reform/assets/*.dc.html` にも保存。

| # | 画面 | 内容 |
| --- | --- | --- |
| 1 | 案件一覧 | ステータス絞り込み、次アポ表示 |
| 2 | ヒアリングシート | きっかけ・予算・希望時期・履歴・キーマン同席・変動リスクメモ ※旧仕様。§7の変更未反映 |
| 3 | 見積作成(松竹梅) | パターン切替、設備グレード比較、内訳自動計算 |
| 4 | 施工日程調整 | 担当者×時間枠のタイムグリッド、日程候補の自動提案 ※旧仕様。§7の変更未反映 |
| 5 | 契約サイン | 説明チェック、手書きサイン ※発注者側のみ。§7で請負者側サインの検討中 |
| 6 | 工事マスタ | 所要時間(分)の設定画面 |

---

## 6. 実装済み機能(`reform-app/`)

Fast系フローが**実際に動くWebアプリ**として動作(スマホ/デスクトップ両対応)。

### 6-1. 画面構成

| Path | 内容 |
| --- | --- |
| `/` | 案件一覧(ステータス絞り込み・失注フィルタ・案件作成) |
| `/projects/[id]/hearing` | ヒアリングシート(予算・希望時期・履歴を含む。§7で刷新済み)。下部から「見積に進む」/「類似の過去見積を検索」 |
| `/projects/[id]/estimate` | 見積作成(松竹梅・設備グレード差し替え・内訳自動計算・受注時粗利の表示・失注登録) |
| `/projects/[id]/schedule` | 施工日時の調整(タイムグリッド・◀▶で施工日変更・工事項目ごとの担当者割り当て・仮押さえ) |
| `/projects/[id]/contract` | 契約(施工日時入り・説明チェック・発注者の手書きサイン+請負者の記名押印・締結) |
| `/projects/[id]/estimate/print` | 見積書帳票(ブラウザ印刷でPDF保存) |
| `/projects/[id]/contract/print` | 工事請負契約書帳票 |
| `/search` | 過去見積の検索(類似度スコア順)。未選択でも「見積に進む」で先へ進める |
| `/catalog` | 商品マスタ(グレード系列) |
| `/works` | 工事マスタ(所要時間=分、標準単価・標準原価=円を設定) |
| `/settings` | 会社情報(会社名・住所・代表者名・印鑑画像・受注時粗利の目標) |

### 6-2. 見積 → PDF出力

- 松竹梅3パターンをタップ切替、設備グレードを比較モーダルで差し替えると内訳・合計を自動再計算
- 見積書・契約書とも「PDFとして保存 / 印刷」ボタンからブラウザ印刷機能でPDF化(見積書はサンプルPDFと同じ構成: 表紙・内訳・工事種別ごとの明細・注意事項)

### 6-3. 過去見積の類似度検索

ルールベースの類似度スコア(8点満点): 工事種別一致+3、築年数帯一致+2、予算帯一致+2、構造一致+1。案件のヒアリング内容から自動採点し、スコア降順で表示。将来はベクトル類似検索(pgvector)に置き換える設計。

### 6-4. 施工日時調整 + Googleカレンダー連携(実装済み・②方式)

- 担当者×時間のタイムグリッド(24時間表示、**スマホ30分単位/タブレット・PC15分単位**に自動切替)
- 施工日は**◀▶の矢印ボタン**で前後に移動(日付ピッカーは廃止・§8-3-4)、開始時刻はグリッドのドラッグで**営業担当が自由に指定**。工事マスタの所要時間(分)からその時刻を起点に工程順で自動割付。自動提案(候補一覧)は削除済み(§7)
- **工事項目ごとに担当者を選択可能**(職種が一致する担当者は「(推奨)」表示。職種が違う担当者にも割り当てられる・§8-3-3)。選んだ担当者の空き状況で衝突判定される
- **Googleカレンダー連携(OAuth・自動登録方式)を実装済み**:
  - 担当者ごとにOAuth同意 → その担当者の空き(FreeBusy API)を実データで取得
  - 仮押さえで各担当者のカレンダーに **【仮】** 予定を自動登録
  - 契約締結で **【確定】** にタイトル更新
  - `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` が未設定の間はデモの空きデータで動作(段階的に有効化できる設計)
  - 実装ファイル: `lib/server/google.ts`(OAuth・FreeBusy・イベント登録)、`app/api/google/*`、`app/api/calendar/*`
  - 連携トークンは暫定でファイル保存(`.data/google-tokens.json`)。DB導入時に置き換え予定

### 6-5. 工事マスタ

工事項目ごとの標準所要時間を**分単位**で編集可能(即保存)。Fast系標準セット(撤去・養生60分→洗面台取付120分→クロス補修60分=計240分/4時間)にはバッジ表示。変更は施工日時の自動割付に即反映。

### 6-6. データ永続化

現状は localStorage(ブラウザ内保存)のみ。「デモデータに戻す」ボタンでリセット可能。DB・認証は未実装。

---

## 7. 直近の変更要望(実装済み)

以下は2026年最新のフィードバックで、**すべて実装済み**(lint・build・E2E動作確認済み)。

### 7-1. ヒアリングシート ✅

1. **予算入力の金額表記化** ✅: 数値入力欄が入力に応じて自動でカンマ区切り金額表記(例: `¥1,234,567`)になるよう実装。内部値は円単位の数値(`Hearing.budget: number | null`)で保持し、表示側で `toLocaleString("ja-JP")` を都度適用
2. **「ご予算の目安」の選択肢チップを削除** ✅: クイック選択チップを廃止し、自由入力の金額欄のみに一本化
3. **希望時期を日付レンジのカレンダー入力に変更** ✅: `<input type="date">` を2つ(開始日・終了日)並べ、ブラウザ標準のカレンダーUIで選択。下に「YYYY年M月D日 〜 YYYY年M月D日」の確認表示も追加(`Hearing.timingFrom` / `timingTo`、`lib/schedule.ts` の `formatFullDateJa`)
4. **過去のリフォーム履歴を年月リスト選択+手入力+複数レコード化** ✅:
   - 年・月をそれぞれ `<select>` で選択、内容は自由入力のテキスト欄
   - 「履歴を追加」ボタンで複数レコードを追加・個別削除可能(`Hearing.history: HistoryRecord[]`)
   - **ANDPAD連携を見据えた設計**: `HistoryRecord.source` を `"manual" | "andpad"` で持たせ、`source: "andpad"` のレコードは入力欄をdisabledにして編集不可(参照のみ)、`"manual"` は通常どおり編集可能。現状は「ANDPADから取得」ボタンでサンプルレコードを1件追加するプレースホルダー実装(実際のANDPAD API連携は未接続。§9の未確定事項に接続ルートの検討を記載)
5. **「キーマン同席」の項目を削除** ✅: `Hearing.keymanTogether` フィールドごと削除

### 7-2. 施工日時調整画面 ✅

6. **カレンダーの自由入力** ✅: 施工日を `<input type="date">`、開始時刻を `<input type="time" step={900}>`(15分刻み)で営業担当が直接指定できるように変更。指定した日時を起点に、工事マスタの所要時間から工程順で自動割付してタイムグリッドに表示(`lib/schedule.ts` の `allocateDay` が `startMin` 引数を受け取るよう変更)
7. **「組める日程の候補」機能を削除** ✅: 自動提案(`findCandidates`)のロジック・UIを削除し、6の自由入力に一本化

### 7-3. 契約画面(請負者側の署名) ✅ → **§8-3-5で請負者の署名は廃止(社印のみ)**

> この節は当時の対応の記録です。第3次フィードバック(§8-3-5)により、**請負者側の手書き署名は削除**され、乙欄は会社名・住所・代表者名・印鑑画像のみになりました。発注者(お客様)の手書きサインは現在も有効です。


8. **請負者(自社)側の署名に対応** ✅: 「都度サイン」「登録済み署名の自動表示」の**両方**を実装。
   - 会社の署名は `useStore().companySignature` としてlocalStorageに永続化(`reform-app.companySignature.v1`、案件をまたいで共有)
   - 未登録の場合は契約画面に手書きサイン欄が表示され、「この署名を登録する」で保存すると以降の**すべての契約で自動的に表示**される(お客様側の署名だけ都度描けば締結できる)
   - 「署名を登録し直す」でいつでも会社署名を更新可能
   - 締結時、その時点の会社署名のコピーを `ContractState.contractorSignature` として案件に保存(後で会社署名を変更しても過去の契約書には影響しない)
   - 契約書PDF帳票(`/projects/[id]/contract/print`)にも甲(発注者)・乙(請負者)双方の署名画像を表示するよう更新
   - 手書きサインの共通ロジックは `components/SignaturePad.tsx` に切り出し、発注者用・請負者用の両方で再利用

> **請負者側サインの法的な考え方(参考・再掲)**: 日本の民法上、契約は当事者双方の意思表示の合致で成立し、書面や署名捺印は契約の成立要件ではないのが原則です。ただし建設工事の請負契約は実務上・信頼性確保の観点から**双方の記名押印(または署名)を交わすのが一般的な慣行**であり、クーリングオフ書面など特定商取引法が絡む場合は交付書面の記載事項が法定されています(押印必須とまでは明記されていません)。したがって「法的に必須」というよりは「実務上・証拠力確保のために双方の署名を残すべき」という位置づけです。この点は最終的に顧問弁護士等へのリーガルチェックを推奨します(`PLANNING.md` §7 の未確定事項にも記載済み)。

---

## 8. 商品マスタの自動登録(カタログ取込)の検証結果

営業が使うメーカーカタログ(デジタルカタログのキャプチャ画像)から、商品マスタ(シリーズ・品番・定価・寸法・画像)を半自動で登録する仕組みの実現可能性を検証した。

### 検証結果(TOTO・タカラスタンダードの実キャプチャで実施)

- **テキスト項目(シリーズ・品番・定価・寸法)の抽出**: 実用レベル。**メーカーごとにカタログのレイアウトが違っても問題なし**(LLMビジョンに「このスキーマで抜いて」と渡す方式のため、座標依存のテンプレート方式と異なりレイアウト自由)
- **商品画像の切り出し**: 最大の難所。LLMは画像領域の座標指定が不安定なため自動切り抜きは非推奨。現実解は下記のいずれか:
  1. 登録画面で人がキャプチャ上をドラッグして矩形を手動切り抜き(半自動・1商品数秒)
  2. メーカー公式サイトから品番で画像を別途取得
  3. まず画像なしで運用開始
- **条件付き価格・注記**(「※特定品番の場合」等)は抽出できるが、**誤登録防止のため人によるレビュー必須**

### マスタ設計上の論点(タカラのレンジフードカタログから判明)

1. **カラー展開品番**(例: `VUA-601AD(▲)` の▲にカラー記号が入る): 商品マスタは「**親商品 + バリエーション(間口×カラー)**」の2階層で持つのがよい
2. **本体と関連部材の関係**: 「本体+必須別売品(排気金具)+加算オプション(電動シャッター等)+サイズ別部材(前面パネル)」が1カタログに同居する。将来的にマスタを「商品(親) ─ バリエーション ─ 関連部材(必須/任意、加算価格)」へ拡張すれば、見積作成時に「本体を選ぶと必須部材が自動で明細に入る」という実務的な動きが可能になる

### 現時点の合意

**まずは現行の商品マスタ仕様(グレード系列のみ)のまま進めてよい**(ユーザー確認済み)。関連部材・バリエーション対応の拡張は、他メーカーの部材データも登録する段階で改めて設計する。

### 想定する実装フロー(未着手)

「キャプチャをアップロード → Claude APIがスキーマ付きJSONで候補を返す → レビュー画面で人がセル単位に確認・修正(自信が低い項目はハイライト) → 承認で商品マスタに登録」。全自動登録にはせず必ず人のレビューを挟む設計。

---

## 8-2. 第2次フィードバックへの対応(実装済み)

### 8-2-1. リフォーム履歴が登録できないバグ ✅

**原因**: 履歴の年月を `"YYYY-MM"` という1つの文字列で保持していたため、「年だけ選んだ」中間状態を保存できず(年か月の片方が欠けると空文字になる実装)、月を選ぶ頃には年が消えていた。

**修正**: `HistoryRecord` の年月を `year: number | null` / `month: number | null` の独立フィールドに分離。旧形式(`yearMonth`)で保存済みのデータは store のマイグレーションで自動変換する。年→月→内容の順に入力しても、リロード後も保持されることをE2Eで確認済み。

### 8-2-2. 見積を自分で作成できるようにする ✅

類似の過去見積が無い場合に、**工事マスタから選んで見積明細を組み立てられる**ようにした。

- 見積画面に「工事明細」セクションを追加。`サンプル見積 / 自作見積` のバッジで現在の状態が分かる
- **「この明細をベースに編集」**(サンプルをコピーして修正)と**「ゼロから作成」**の2つの入口
- 「工事マスタから追加」でモーダルから工事項目を選択 → 明細行として追加(名称・仕様・単位・単価が初期値として入る)。「空の明細」で手入力の行も追加可能
- 入力項目は**見積書サンプルPDFの構成に準拠**: 工事種別(セクション)/ 名称 / 仕様 / 数量 / 単位 / 単価 / 金額(自動計算)
- 工事種別ごとに小計を表示し、サンプル見積書と同じセクション順(共通→解体・撤去→基礎→大工→内装→電気→水道→住宅設備)に並ぶ
- 見積書PDFにも**数量・単位・単価の列**を追加してサンプルの体裁に合わせた
- 見積書・契約書の表紙項目として**工事場所・支払条件**を見積画面で入力できるようにした(従来はプレースホルダー固定)
- **工事マスタに単価(円)・単位・標準仕様を追加**。`/works` で所要時間(分)と標準単価(円)の両方を編集できる
- 見積明細に工事マスタ由来の行があれば、**施工日程の所要時間もその明細から自動集計**される(見積と工程が連動)

### 8-2-3. 施工日時をドラッグで登録 ✅

タイムグリッド上を**直接ドラッグして施工時間帯を指定**できるようにした。

- グリッドを押した位置が開始時刻になり、ドラッグ中はリアルタイムで工程ブロックが追従(15分スナップ)
- カレンダーとして自然な挙動になるよう、**ドラッグ範囲の先頭を開始時刻**とする(下方向にドラッグしても開始時刻は動かない/上方向なら先頭が繰り上がる)
- 従来の時刻入力欄は廃止。日付はカレンダー入力または ◀▶ で変更
- 選択中の時間帯は常時ヘッダーに表示(例: `8:00 〜 12:00`)

### 8-2-4. 請負者の署名を会社情報として登録 ✅ → **署名欄は§8-3-5で削除済み**


`/settings`(会社情報)を新設。**一度登録すればすべての見積書・契約書に自動表示**される。

- 登録項目: **会社名・住所・代表者名・印鑑画像・代表者の手書き署名**(※手書き署名は§8-3-5で削除。現在は会社名・住所・代表者名・印鑑画像の4項目)
- 印鑑画像はファイルアップロード対応(長辺400pxに自動縮小してlocalStorageの容量を圧迫しないようにしている)
- 契約画面の「乙(請負者・自社)」欄に会社名・住所・代表者名・社印を表示。未登録の場合は設定画面への導線を出す
- 契約書PDFの乙欄、見積書PDFのヘッダーにも同じ情報+社印を表示
- 締結時に**その時点の会社情報のスナップショット**(`contract.contractorProfile`)を案件に保存するため、後から会社情報を変更しても過去の契約書は変わらない
- 契約締結の条件は「説明3項目のチェック + 発注者の署名 + 会社情報(会社名)の登録」。請負者側は毎回サインする必要がない

---

## 8-3. 第3次フィードバック(実装済み / v0.5)

2026-08-30 にいただいたフィードバック。8-3-6(判子画像の保存)を除き、**すべて実装・動作確認済み**。

### 8-3-1. ヒアリング画面に「見積に進む」ボタンを追加 — 実装済み

ヒアリング画面の最下部に、**「見積に進む」(主ボタン)+「類似の過去見積を検索」(副ボタン)**を縦に並べた。過去見積を探す必要がないケースで遠回りにならない。

- 「見積に進む」→ ステータスを `estimating`・次アクションを「見積作成中」に更新し、`/projects/[id]/estimate` へ遷移(契約済み案件のステータスは変更しない)
- 実装: `app/projects/[id]/hearing/page.tsx` の `goEstimate()`

### 8-3-2. 過去見積検索画面に「見積に進む」ボタンを追加(未選択でもエラーにしない) — 実装済み

検索結果の一覧下に「見積に進む」ボタンを**常設**した。**過去見積を1件も選択していなくても押せる**(選ばなかった場合は、その案件の見積画面をそのまま開く)。

- 案件に紐づかずに検索画面へ来た場合(サイドバーの「見積検索」から直接など)は、ボタンのラベルが「案件一覧へ戻る」になり `/` へ戻るだけで、**エラーにはならない**
- 各カードの「この見積をベースに作成」も同じ処理(`goEstimate`)を呼ぶ
- 実装: `app/search/SearchClient.tsx`

### 8-3-3. 施工担当者を選べるようにする — 実装済み

`workerForTrade()` が職種に一致した**最初の1人**を返していたため、実質「山本」しか割り当たらなかった。**工事項目ごとに担当者を選べる**ようにした。

- 施工日時調整画面に「担当者の割り当て」カードを追加。工事項目ごとにプルダウンで担当者を選ぶ
- 選択肢は**全担当者**。職種が一致する担当者には「(推奨)」を付け、先頭に並べる(職種が違う担当者にも割り当て可能)
- 既定のままにしたい項目は「既定(◯◯)」を選んでおけばよい
- 選んだ担当者の空き状況(FreeBusy / デモ予定)で衝突判定・後ろ倒しが行われる
- 実装: `lib/schedule.ts` に `workersForTrade()` / `workerCandidates()` を追加し、`allocateDay(tasks, busyByWorker, startMin, assignments)` に割当マップを渡す形にした。画面側は `app/projects/[id]/schedule/page.tsx` の `assignments` state

### 8-3-4. 施工日の変更を日付入力から矢印操作に変更 — 実装済み

「施工日」の `<input type="date">` カードを**削除**し、施工日の変更は**◀▶ の矢印ボタンのみ**にした。

- 矢印は枠付きのボタン(タップ領域 44px 以上)にし、日付の下に「矢印で施工日を変更」の補助テキストを表示
- 実装: `app/projects/[id]/schedule/page.tsx`

### 8-3-5. 会社情報の「代表者の署名」欄を削除 — 実装済み

請負者側の表示は**会社名・住所・代表者名・印鑑画像(社印)**のみとした。

- `/settings` の「代表者の署名」カードと、契約書プレビューの署名画像を削除
- 契約画面・契約書PDFの乙欄からも署名画像を削除(契約書PDFの見出しは「乙(請負者)記名押印」に変更)
- **発注者(お客様)の手書きサインはそのまま残る**(契約画面の署名パッド)
- データ構造: `CompanyProfile.signature` と `ContractState.contractorSignature` を削除。保存済みの会社情報に残っている旧 `signature` は、読み込み時に `normalizeCompany()` で読み捨てる(`lib/store.tsx`)

### 8-3-6. アンドパッドの判子画像の保存 — **未完了(ファイル待ち)**

チャットに貼っていただいた判子画像は**会話内の画像として渡されており、ファイルとして取り込めなかった**ため、リポジトリに保存できていない。置き場所として `reform-app/public/assets/` を用意し、手順を `reform-app/public/assets/README.md` に記載した。

- **推奨**: アプリの「会社情報」画面 →「印鑑画像をアップロード」から登録すれば、そのまま見積書・契約書に反映される(実装済み・リポジトリへの配置は不要)
- リポジトリに既定画像として持たせたい場合は、`reform-app/public/assets/company-seal.png` として配置いただく(アプリ側で既定値として読む対応は未実装)

### 動作確認(v0.5)

`npm run lint` / `npx tsc --noEmit` / `npm run build` を通したうえで、Playwright(Chromium)で以下を確認済み。

| 確認項目 | 結果 |
| --- | --- |
| ヒアリング画面の「見積に進む」→ 見積画面へ遷移 | OK |
| 検索画面で何も選ばずに「見積に進む」→ 見積画面へ遷移(エラーなし) | OK |
| 案件なしで検索画面を開いた場合 → 「案件一覧へ戻る」表示・エラーなし | OK |
| 担当者プルダウンの選択肢 = 既定(山本)/山本(推奨)/田口設備/川井内装 | OK |
| 「既存設備の撤去・養生」を川井内装に変更 → タイムグリッドの川井内装列へ移動し、後続工程が空き時間に合わせて再配置 | OK |
| 施工日時画面に `input[type=date]` が存在しない / ▶ で 9/1(火) → 9/2(水) | OK |
| 会社情報画面に署名パッド(canvas)が無い / 契約画面には発注者の署名パッドが残る | OK |

---

## 8-4. 業界ナレッジ(川鍋 v1.0)の読み込みと更新方針 — 2026-09-01

`260820_業界知識_リフォーム_川鍋 v1.0`(全16章)を読み込み、現行 v0.5 とのギャップを整理して
**[`INSIGHTS_AND_ROADMAP.md`](./INSIGHTS_AND_ROADMAP.md)** にバックログ化した。要点のみ再掲する。

- 最大のギャップは **見積に原価・粗利の概念が無いこと**。ナレッジのKGIは「利益改善」、ボトルネックは「粗利の毀損(受注時・完工時の2段階)」であり、受注時粗利を守る機能が本プロダクトの中核になるべき。
- 消費者調査(n=1,000)は「**現調後失注の67%は価格以外**」「**初動3日が分水嶺**」「**問い合わせ社数は7割が1社のみ**」を示す。勝負は安さではなく**速さ・提案・態度の標準化**で、いずれもプロダクトで支援できる。
- ナレッジのAI検討の本命「**F 類似案件サジェスト**(粗利毀損のない見積雛形を作成者名付きで提示)」は、現行の「類似の過去見積検索」の正常進化形。今はメタデータ類似止まりなので、**明細レベル＋作成者＋完工粗利率**に引き上げる。
- 契約後の指標(完工粗利・発注・入金・リピート)はスコープ外とし、**ANDPADへ受け渡す前提**で項目を揃える。
- 着手順の推奨: **v0.6 = 見積の原価・粗利 + 失注理由の型化** → v0.7 概算見積・リードタイム計測 → v0.8 類似案件サジェスト → v0.9 追加提案・顧客提示 → v1.0 管理者/経営者レイヤー。

判断待ち: ①「キーマン(決裁者)」項目の再導入可否(第2次FBで削除済みだが、ナレッジは決裁者の見極めを重視) ②標準原価の入手経路 ③概算計算の係数 ④目標値(粗利34〜35%等)を会社ごとに設定可能にするか。

> **取扱注意**: 入力資料は社外秘(クライアント個社名・実数値、Looker実測値、グレード要件)を含むため、`INSIGHTS_AND_ROADMAP.md` にはそれらを転記していない。

---

## 8-5. v0.6 実装(業界ナレッジ P0-1 / P0-3) — 2026-09-01

`INSIGHTS_AND_ROADMAP.md` の P0 のうち、最優先の2本を実装した。

### 8-5-1. 見積の原価・粗利(P0-1)

業界ナレッジの中心概念「利益 = 売上 × 粗利率 × 件数」「粗利は受注時・完工時の2回減る」が、
プロダクトに存在しなかったため追加した。

- **データ**: `WorkItem.costPrice`(標準原価)、`Product.costPrice`(仕入額)、`EstimateLine.costUnitPrice`(原価単価)、`WorkLine.cost`(サンプル見積の原価)
- **計算**: `calcPattern()` が `cost` / `grossProfit` / `marginRate` を返す。**受注時粗利 = 値引き後の税抜提示額 − 原価合計**。諸経費は売上側に乗せるため原価に含めず、値引きはそのまま粗利を削る
- **見積画面**: 「受注時粗利(社内用)」カードで売上・原価・粗利額・粗利率と、目標に対する達成度バーを表示。目標割れは警告色。松竹梅の各ボタンと設備グレード比較にも粗利率を表示し、差し替えると即再計算される
- **工事マスタ**: 標準原価を編集可能にし、項目ごとの粗利率を表示
- **会社情報**: 受注時粗利の目標(%)を設定可能(既定34%)。ナレッジのグレード要件は契約時34〜35%が目安だが、v2との差分が未確認のサンプル値のため**会社ごとに設定できる形**にした
- **帳票**: 原価・粗利は**社内用**。見積書PDF・契約書PDFには一切出力しない(E2Eで確認済み)

### 8-5-2. 失注理由の型化(P0-3)

ナレッジ §10-2 の BEFORE/AFTER 表をそのまま仕様にした。

- 見積画面・契約画面の下部に「この案件を失注として登録する」
- **区分**(他決/検討中止)×**理由**(価格/時期/プラン/その他)の2軸。他決なら**競合先**、理由が「その他」なら**備考を必須**(自由記述だけにすると分析できなくなるため)
- 案件一覧に「失注」フィルタと理由バッジ。失注の取り消しも可能
- 失注案件を開くと、失注パネルのある見積画面へ遷移する

### 8-5-3. 動作確認(v0.6)

`npm run lint` / `npx tsc --noEmit` / `npm run build` を通したうえで、Playwright(Chromium)で確認。

| 確認項目 | 結果 |
| --- | --- |
| 竹プランの粗利表示(売上¥1,810,000 / 原価¥1,138,000 / 粗利¥672,000 = 37.1%) | OK |
| 松に切替 → 38.1%・¥820,000 に再計算 | OK |
| 松竹梅ボタンの粗利率バッジ(38.1% / 37.1% / 36.2%) | OK |
| 目標を50%に変更 → 「11.9pt 不足しています(あと ¥255,000)」の警告 | OK |
| 見積書PDFに「原価」「粗利」の文字が出ない | OK |
| 理由「その他」で備考未入力 → 登録ボタンが押せない | OK |
| 他決(価格)＋競合先で登録 → 一覧の失注フィルタに理由付きで表示 | OK |
| 失注の取り消し → 登録ボタンに戻る | OK |

---

## 9. 未確定事項(引き続き未決)

`PLANNING.md` からの持ち越し分含む:

- [ ] カタログ連携の情報源(メーカー提供データ/CSV/手入力)。§8の半自動取込を本実装するか
- [ ] 電子契約: 手書きサイン継続 or クラウドサイン等のAPI連携に切り替えるか
- [ ] ANDPAD API: パートナー契約・API仕様の入手ルート(公式パートナー申請が必要)。接続でき次第、履歴の「ANDPADから取得」ボタン(現状はサンプルデータを追加するだけのプレースホルダー)を実際のAPI呼び出しに置き換える(§7-1-4)
- [ ] 訪問販売該当性(クーリングオフ書面)と契約書式のリーガルチェック(§7-3参照)。現状は**発注者=手書きサイン、請負者=記名+社印**という形(§8-3-5)。この形で足りるかの最終確認が中心
- [ ] DB導入時期(現状localStorageのみ。会社情報・印鑑画像・Googleカレンダー連携のトークン保存もローカル/ファイル暫定)
- [ ] 見積の松竹梅と自作明細の関係(現状は明細が全パターン共通で、設備グレードのみパターンで差し替わる。パターンごとに明細自体を変えたい場合は要拡張)
- [ ] 標準原価の入手経路(現状は工事マスタへ手入力。過去の実行予算からの取込 or ANDPAD連携に置き換えるか)
- [ ] 「キーマン(決裁者)」項目の再導入可否(第2次FBで削除済み。ナレッジ §2 は決裁者の見極めを重視)
- [ ] 概算見積(v0.7予定)の計算方式。ナレッジ記載の「人工と材料費が概ねイコール」方式でよいか
- [ ] 商品マスタの「親商品 ─ バリエーション ─ 関連部材」への拡張(§8のタカラのカタログで判明した論点。他メーカーの部材を登録する段階で設計)

---

## 10. 環境・起動方法(再掲)

```bash
git clone -b claude/reform-product-planning-lv0tli https://github.com/marushuuu/inbound.git
cd inbound/reform-app
docker compose up --build
# → http://localhost:3000
```

更新を取り込む場合:

```bash
cd inbound
git pull
cd reform-app
docker compose down
docker compose up --build
```

Googleカレンダー連携を有効化する場合は `reform-app/.env.example` を `.env.local` にコピーし、`GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` を設定(手順は同ファイルとREADME参照)。
